{% extends 'admin/admin.html.twig' %}

{% block title %}Department index{% endblock %}

{% block body %}
    <h1>Department index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom du département</th>
                <th>Numéro du département</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for department in departments %}
            <tr>
                <td>{{ department.name }}</td>
                <td>{{ department.number }}</td>
                <td>
                    <a href="{{ path('department_show_admin', {'id': department.id}) }}">show</a>
                    <a href="{{ path('department_edit_admin', {'id': department.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('department_new_admin') }}">Create new</a>
{% endblock %}
