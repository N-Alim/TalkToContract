 {% extends "admin/admin.html.twig" %}
 {% block title %}OfferType{% endblock %}


{% block body %}
    <h1>OfferType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offer_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ offer_type.label }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ offer_type.createdAt ? offer_type.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modified_at</th>
                <td>{{ offer_type.modifiedAt ? offer_type.modifiedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offer_type_index_admin') }}">back to list</a>

    <a href="{{ path('offer_type_edit_admin', {'id': offer_type.id}) }}">edit</a>

    {{ include('/admin/offer_type/_delete_form.html.twig') }}
{% endblock %}
