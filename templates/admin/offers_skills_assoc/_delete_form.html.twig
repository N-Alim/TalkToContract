<form method="post" action="{{ path('offers_skills_assoc_delete_admin', {'id': offers_skills_assoc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offers_skills_assoc.id) }}">
    <button class="btn">Delete</button>
</form>
